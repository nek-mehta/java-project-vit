# java-project-vit
A console-based Java app for logging and categorizing personal expenses, with monthly reports and CSV export. Set per-category budget limits and get alerts when one is crossed, backed by a background thread that re-checks every 15 seconds. Data auto-saves between sessions. Built with OOP, Collections, File I/O and multithreading.
